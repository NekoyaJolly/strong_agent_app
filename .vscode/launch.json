{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run agent (dev)",
      "type": "pwa-node",
      "request": "launch",
      "program": "${workspaceFolder}/src/index.ts",
      "runtimeArgs": ["-r", "dotenv/config"],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}